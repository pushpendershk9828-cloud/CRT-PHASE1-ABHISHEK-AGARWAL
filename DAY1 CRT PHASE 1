#include ‹iostream>
using namespace std;
int main() {
double celcius;
double fahrenheit;
cin >> celcius;
fahrenheit = (celcius* 9.0 / 5.0) + 32;
cout « fahrenheit « endl;
return 0;
}

LOCAL AND GLOBAL VARIABLE

#include ‹iostream>
using namespace std;

int globalvariable = 100;

int main() {
int localvariable;
int result;
cin > localvariable;

result=Colobalvarioble / localvarabte):
cout < result < endl;
return 0;

Give sides of triangle are equilateral or not

int main(){
int side1,side2, side3;
CLn› sidel >›side2 > side3;
bool isEqu = (side1 › 0) && (sidel — side2) && (side2 - side3);

cout < isEqu < endl; 
return 0;

swapping of two variable

#include <iostream> 
using namespace std;
int main {
int a, b,
cin >> a > b;

// logic to swap variables value
int temp = a;
a=b;
b = temp;

//printing the values
cout « a « "\n" «b « endl;
return 0;

login credentials

#include ‹lostream›
using namespace std;
int main() {
int userId = 1234;
int userPassword = 0000;

int inputId;
int inputPassword;

CLn>> inputId>> inputpassword;

bool isGranted = (userIdinputId) && (userPassword= inputPassword);
CoUtくく"Access Granted"<<isgranted << endl;
cout<< "Access denied" <<isGranted << endl;
return0;
